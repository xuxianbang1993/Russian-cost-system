# v0.1.0 Phase 5.2 Checkpoint

## 完成内容
- 已完成 Step 5 的 `Phase 5.2`，左侧输入面板已从占位态推进到可交互状态。
- 已实现 `TierSelector`，支持 4 档营收切换、当前档位高亮、`tier4` 的一般税制提示。
- 已实现 `CostInputPanel`，包含年度营业额、商品核心参数、附加参数、5 项经营支出，以及无商品时的空状态。
- 已将 `TierSelector` 与 `CostInputPanel` 接入 `CalculatorShell` 左侧面板。
- 已新增组件测试并通过验证。
- 已顺手修复阻塞质量门禁的既有问题，确保 lint / test / typecheck 全绿。

## 关键文件变更
| 文件 | 变更内容 | 备注 |
|------|---------|------|
| `src/components/calculator/TierSelector.tsx` | 4 档 pill 切换、active 样式、`SET_TIER` 分发 | `tier4` 带一般税制提示 |
| `src/components/calculator/CostInputPanel.tsx` | 年度营业额、商品参数、5 项支出输入 | 数字输入使用 `font-mono` |
| `src/components/calculator/CalculatorShell.tsx` | 左侧接入 `TierSelector` 和 `CostInputPanel` | 右侧仍为 `Phase 5.4` 占位 |
| `src/__tests__/component/calculator/TierSelector.test.tsx` | 校验 4 档渲染、active 样式、切换分发、OSNO 提示 | 新增 |
| `src/__tests__/component/calculator/CostInputPanel.test.tsx` | 校验营业额、费用字段、商品参数、空状态 | 新增 |
| `src/components/auth/AuthForm.tsx` | 去除 render path ref 访问 | 修复 lint 质量门禁 |
| `src/contexts/calculator/CalculatorProvider.tsx` | 调整 memoization 以适配 React Compiler lint | 修复 lint 质量门禁 |
| `eslint.config.mjs` | 忽略 `coverage/**` | 避免无关 lint 噪音 |
| `src/lib/calc/engine.ts` | 小幅 warning 清理 | 无行为变更 |
| `src/__tests__/unit/calc/engine.test.ts` | 对应 warning 清理 | 无行为变更 |

## 已完成验证
| 命令 | 结果 |
|------|------|
| `pnpm lint` | 通过 |
| `pnpm test:run` | 通过，`75` 个测试全部通过 |
| `.\\node_modules\\.bin\\tsc.cmd --noEmit` | 通过 |

## 当前状态判断
- `Phase 5.2` 已达到退出条件，可以进入 `Phase 5.3`。
- `Step 5` 整体尚未完成，因为右侧结果面板、商品管理、批量模拟、保存测算等阶段仍未实现。

## 已知问题 / 风险
- 当前目录不是 git 工作树，后续继续开发前不要假设有可用分支状态。
- PowerShell 输出可能出现中文乱码；读写文档时显式使用 UTF-8 更稳。
- 右侧区域仍是占位文案，若继续开发前端主流程，下一优先级不是美化，而是先补 `Phase 5.3` 的商品管理。

## 下一步
- 进入 `Phase 5.3: ProductManager + ProductFormModal + Server Actions`
- 目标文件：
- `src/lib/schemas/product.ts`
- `src/app/actions/products.ts`
- `src/components/calculator/ProductFormModal.tsx`
- `src/components/calculator/ProductManager.tsx`
- `src/__tests__/component/calculator/ProductManager.test.tsx`
- `src/components/calculator/CalculatorShell.tsx`
- 完成后再次运行：
- `pnpm lint`
- `pnpm test:run`
- `.\\node_modules\\.bin\\tsc.cmd --noEmit`
