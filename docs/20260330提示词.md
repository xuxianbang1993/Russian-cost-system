# v0.1.0 Phase 5.2 续接提示词（2026-03-30）

> 用途：新会话里先读取这份文档，可在最短路径内恢复 Step 5 当前上下文。

## 立即执行：恢复上下文
1. 读取 `docs/v0.1.0-checkpoint-phase5.2.md`
2. 读取 `MEMORY.md`
3. 读取 `DEVELOPMENT_STRATEGY.md`
4. 读取 `docs/plans/2026-03-28-step5-implementation-plan.md`
5. 读取 `docs/plans/2026-03-28-step5-calculator-frontend-design.md`
6. 如果这轮会改 Next.js 相关页面或能力，再读取 `node_modules/next/dist/docs/` 对应指南

## 当前进度
- `Step 5 / Phase 5.1-5.2` 已完成并验证
- 已落地：
- `src/components/calculator/TierSelector.tsx`
- `src/components/calculator/CostInputPanel.tsx`
- `src/components/calculator/CalculatorShell.tsx`
- `src/__tests__/component/calculator/TierSelector.test.tsx`
- `src/__tests__/component/calculator/CostInputPanel.test.tsx`
- 已顺手修复若干质量门禁问题：
- `src/components/auth/AuthForm.tsx`
- `src/contexts/calculator/CalculatorProvider.tsx`
- `eslint.config.mjs`
- `src/lib/calc/engine.ts`
- `src/__tests__/unit/calc/engine.test.ts`
- 当前右侧结果区仍是 `Phase 5.4` 占位，不要误判为完整前端已做完

## 本轮验证结果
- `pnpm lint` 通过
- `pnpm test:run` 通过，当前 `75` 个测试全部通过
- `.\\node_modules\\.bin\\tsc.cmd --noEmit` 通过

## 下一步直接做
1. 按 `DEVELOPMENT_STRATEGY.md` 的 `SPEC -> TEST -> IMPL -> VERIFY` 继续 `Phase 5.3`
2. 先补商品管理相关测试和 schema
3. 再实现商品 CRUD 的 Server Actions 和表单弹窗
4. 将 `ProductManager` 接入 `CalculatorShell`
5. 完成后重新跑：
   - `pnpm lint`
   - `pnpm test:run`
   - `.\\node_modules\\.bin\\tsc.cmd --noEmit`

## 这轮要做的文件
- `src/lib/schemas/product.ts`
- `src/app/actions/products.ts`
- `src/components/calculator/ProductFormModal.tsx`
- `src/components/calculator/ProductManager.tsx`
- `src/__tests__/component/calculator/ProductManager.test.tsx`
- `src/components/calculator/CalculatorShell.tsx`

## 质量红线
- 所有颜色只允许使用 Design Tokens
- 所有金额、税率、百分比、关键数字输入与展示使用 `font-mono`
- 默认优先 Server Components，把 `'use client'` 下推到叶子组件
- 不允许跳过测试或类型检查
- 若最终结论涉及最新外部信息，要在收尾前联网验证

## 额外提醒
- PowerShell 读中文时可能乱码，显式使用 UTF-8
- 当前目录不是 git 工作树，按文档和实际文件状态推进即可
